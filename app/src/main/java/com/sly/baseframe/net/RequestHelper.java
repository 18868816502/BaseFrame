package com.sly.baseframe.net;

import java.io.File;
import java.util.List;
import java.util.Map;

import io.reactivex.Observable;

public class RequestHelper {
//    public static Observable<LoginRes> login(LoginRequestData user) {
//        return RequestManager.getInstance().getRetrofit().create(ApiLogin.class)
//                .request(user)
//                .compose(RxSchedulers.<LoginRes>io_main());
//    }
//
//    public static Observable<IsRegisterRes> getIsRegister(String url, Map<String, String> params) {
//        return RequestManager.getInstance().getRetrofit().create(ApiIsRegister.class).request(RequestManager.parameterToUrl(url, params)).compose(RxSchedulers.<IsRegisterRes>io_main());
//    }
}
